# Local development → copy to `.env` and fill in.
# Production → set the same vars in Vercel → Settings → Environment Variables.

RESEND_API_KEY=re_yourkey_here
BOOKING_TO_EMAIL=clinic@yourdomain.com
BOOKING_FROM_EMAIL=bookings@yourdomain.com
